# `isochronic-vagrant-opentripplanner`

## Preparation

Clone `yinshanyang/singapore-gtfs` for the GTFS files.

Zip the `.txt` files into `gtfs.zip`.

Copy `gtfs.zip` to `~/data` in `isochronic-vagrant-opentripplanner`.

